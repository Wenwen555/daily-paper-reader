---
title: "GIFT: Global Irreplaceability Frame Targeting for Efficient Video Understanding"
title_zh: GIFT：面向高效视频理解的全局不可替代性帧定位
authors: "Junpeng Ma, Sashuai Zhou, Guanghao Li, Xin Gao, Yue Cao, Hengyu Zeng, Yuxiang Yan, Zhibin Wang, Jun Song, Bo Zheng, Shanghang Zhang, Jian Pu"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25072v1"
tags: ["query:lvlm"]
score: 9.0
evidence: 高效视频理解的帧选择策略
tldr: "视频大语言模型在处理密集帧时计算成本高昂，现有关键帧选择方法因贪婪决策和解耦评估易陷入局部最优。本研究提出GIFT框架，通过评估帧的固有不可替代性来无训练地选择关键帧：首先引入定向多样性量化条件相关性下的独特性，形成统一不可替代性评分；随后采用预算感知细化策略自适应迭代地优先选择核心高评分帧并扩展时间上下文。实验表明，GIFT在长视频基准上相比均匀采样实现了最高12.5%的平均性能提升。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25072v1/fig-001.webp\", \"caption\": \"Figure 1. Average accuracy of LLaVA-Video-7B on Video-MME, LongVideoBench and MLVU for different methods: uniform sampling, BOLT [30], AKS [40], and our GIFT.\", \"page\": 1, \"index\": 1, \"width\": 482, \"height\": 353}, {\"url\": \"assets/figures/arxiv/2603.25072v1/fig-002.webp\", \"caption\": \"Table 1. Comparison of state-of-the-art methods across video understanding benchmarks on LLaVA-Video-7B. Our GIFT consistently outperforms competing approaches across all frame budgets, demonstrating superior performance especially under tight budget constraints. Best results are highlighted in bold, second best underlined.\", \"page\": 6, \"index\": 2, \"width\": 1026, \"height\": 665}, {\"url\": \"assets/figures/arxiv/2603.25072v1/fig-003.webp\", \"caption\": \"Table 2. Performance comparison of state-of-the-art VLMs with and without our GIFT enhancement. GIFT delivers consistent and significant performance gains across all base models, benchmarks and frame budgets (8 and 32), demonstrating its strong generalizability as a model-agnostic, plug-and-play solution.\", \"page\": 7, \"index\": 3, \"width\": 1029, \"height\": 444}, {\"url\": \"assets/figures/arxiv/2603.25072v1/fig-004.webp\", \"caption\": \"Table 3. Ablation study on the refinement batch size B for our Budget-Aware Refinement (BAR) module. Frames are selected iteratively in batches of B in BAR module. Vanilla represents the performance of uniform sampling for comparison.\", \"page\": 8, \"index\": 4, \"width\": 483, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2603.25072v1/fig-005.webp\", \"caption\": \"Figure 3. Ablation study of GIFT’s modules on LLaVA-Video (Frame Budget:32). The y-axis shows performance relative to Uniform Sampling (100%). “Standard Diversity” refers to using the decoupled evaluation criteria with standard diversity instead of our proposed directed diversity. “Without BAR” refers to disabling the Budget-Aware Refinement module.\", \"page\": 8, \"index\": 5, \"width\": 475, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2603.25072v1/fig-006.webp\", \"caption\": \"Figure 2. The overall framework of our GIFT. Given an input video and user query, GIFT first calculates each frame’s query-relevance(r) and directed diversity(d) to quantify its irreplaceability(detailed in the left panel). The core of GIFT is a two-stage selection process (detailed in the right panel). Stage 1 performs the initial selection by identifying frames with high scores (s = r × d). If the sampling budget (K) exceeds batch-size (B), the Budget-Aware Refinement in Stage 2 is triggered. This stage iteratively Selects a batch of B frames, Removes them, and Updates the d for all remaining frames until the budget is met. This iterative update process continuously Releases previously suppressed frames, progressively building a rich temporal context.\", \"page\": 4, \"index\": 6, \"width\": 1029, \"height\": 569}]"
motivation: 现有视频大模型计算成本高，关键帧选择方法易陷入局部最优并引入噪声帧。
method: 提出全局不可替代性框架（GIFT），结合定向多样性和预算感知细化策略，无训练地选择关键帧。
result: "在长视频基准测试中，相比均匀采样，GIFT在LLaVA-Video-7B上平均提升最高达12.5%。"
conclusion: GIFT通过全局不可替代性评估和预算感知优化，显著提升了视频理解效率与性能。
---

## 摘要
视频大语言模型（VLMs）在视频理解方面取得了显著成功，但处理密集帧所带来的巨大计算成本严重限制了其实际应用。现有方法通过选择关键帧来缓解这一问题，但其贪婪决策机制，结合对相关性和多样性的解耦评估，常常陷入局部最优，导致错误地选择不相关的噪声帧。为应对这些挑战，我们提出GIFT：全局不可替代性帧定位，这是一种新颖的无训练框架，通过评估帧的内在不可替代性来选择帧。具体而言，我们首先引入定向多样性来量化一个帧在相关性条件下的独特性，这使我们能够制定一个统一的不可替代性分数。随后，我们的预算感知优化策略采用一种自适应迭代过程：首先确保一组具有最高不可替代性的核心帧集合；然后随着预算的增加，将优先级转向围绕这些选择构建关键的时间上下文。大量实验表明，与均匀采样相比，GIFT在LLaVA-Video-7B的长视频基准测试中实现了最高平均12.5%的性能提升。

## Abstract
Video Large Language Models (VLMs) have achieved remarkable success in video understanding, but the significant computational cost from processing dense frames severely limits their practical application. Existing methods alleviate this by selecting keyframes, but their greedy decision-making, combined with a decoupled evaluation of relevance and diversity, often falls into local optima and results in erroneously selecting irrelevant noise frames. To address these challenges, we propose GIFT: Global Irreplaceability Frame Targeting, a novel training-free framework that selects frames by assessing their intrinsic irreplaceability. Specifically, we first introduce Directed Diversity to quantify a frame's uniqueness conditioned on relevance, which allows us to formulate a unified irreplaceability score. Subsequently, our Budget-Aware Refinement strategy employs a adaptive iterative process that first secures a core set of frames with the highest irreplaceability, and then shifts its priority to building crucial temporal context around these selections as the budget expands. Extensive experiments demonstrate that GIFT achieves a maximum average improvement of 12.5% across long-form video benchmarks on LLaVA-Video-7B compared to uniform sampling.

---

## 论文详细总结（自动生成）

# GIFT: Global Irreplaceability Frame Targeting for Efficient Video Understanding 论文总结

## 1. 核心问题与整体含义
*   **研究背景**：视频大语言模型（VLMs）在处理长视频时，需要处理大量密集帧，导致计算成本（推理延迟和内存消耗）极高，限制了其在实际资源受限场景中的应用。
*   **现有方法局限**：为降低计算量，现有方法通常采用关键帧选择策略。但主流方法存在两大根本缺陷：
    1.  **短视的贪婪决策**：采用逐步、局部最优的选择方式，容易陷入局部最优解。
    2.  **解耦的评价标准**：将查询相关性和内容多样性视为两个独立、需要手动权衡的目标。这种分离的评估方式在追求多样性时，常常牺牲关键的时间连贯性，并可能引入不相关的噪声帧。
*   **核心动机**：为了摆脱上述短视且解耦的范式，本文提出从全局优化的新视角出发，直接评估每个帧的**内在不可替代性**。

## 2. 方法论
GIFT是一个无训练的关键帧选择框架，其核心思想是直接回答一个全局性问题：“对于一个给定的帧，是否存在一个更优的替代品？”如果一个帧存在视觉上相似且查询相关性更高的“更优替代品”，则该帧是可替代的、贡献度低的。

### 核心流程分为两个阶段：
1.  **基于定向多样性的不可替代性量化**
    *   **查询相关性 (r)**：计算帧嵌入与查询嵌入之间的余弦相似度。
    *   **定向多样性 (d)**：这是本文的关键创新。它衡量一个帧相对于其“潜在替代品集合”（即所有比它相关性更高的其他帧）的唯一性。具体定义为该帧到其潜在替代品集合中所有帧的最小视觉特征距离。如果该集合为空（即该帧是最相关的），则赋予最大可能距离值。
    *   **不可替代性分数 (s)**：最终分数为 `s = r × d`。高分意味着该帧既高度相关又难以被其他更相关的帧所替代。

2.  **预算感知细化**
    *   **目的**：解决静态不可替代性评分在需要时间连贯性的任务（如分析连续动作）以及预算增加时可能带来的问题（例如过度抑制关键事件周围的相邻重要帧）。
    *   **策略**：采用迭代的“选择-移除-重新评估”过程（见算法1）。
        *   初始时选择一批具有最高 `s` 分的核心关键帧。
        *   将这些已选帧从候选池中移除。
        *   重新计算剩余所有候选帧的定向多样性 `d`（因为移除了最强的抑制源），从而更新其不可替代性分数 `s`。
        *   重复此过程直到选满预算K个帧。
    *   **效果**：随着预算增加，算法能自然地释放对已选关键事件周围重要上下文邻居的抑制，从而从捕获最关键的单点信息过渡到重建完整的事件叙事流。

## 3. 实验设计
*   **基准数据集**：
    *   MVBench
    *   LongVideoBench
    *   MLVU
    *   VideoMME (不含主观题)
*   **对比方法**：
    1.  Uniform Sampling (均匀采样)：基线方法。
    2.  BOLT (CVPR2025)：使用逆变换采样平衡相关性和时间多样性。
    3.  AKS (CVPR2025)：通过递归分区算法自适应选择关键片段以优化相关性与覆盖率权衡。
*   **评估模型与设置**：
    *   主要评估平台为 LLaVA-Video-7B。
    *   为验证泛化性，还在 VILA-V1.5-8B, LLaVA-OneVision-7B, Qwen2.5-VL-7B, VideoLLaMA3-7B上进行了测试。
    *   统一预采样128个候选应用GIFT及其他对比方法。使用预训练的SigLIP模型提取视觉和文本嵌入以计算相似度。

## 4. 资源与算力
论文未明确说明训练时长或具体GPU数量。仅在“Implementation Details”部分提到：“All experiments are conducted on NVIDIA H100-SXM5-80GB GPUs.” （所有实验均在NVIDIA H100-SXM5-80GB GPU上进行）。由于GIFT是无训练框架，其主要开销在于推理和特征提取阶段。

## 5. 实验数量与充分性
实验设计较为全面和客观：
*   **主实验组数**：
    1.  在LLaVA-Video-7B上测试了4种不同预算（32,16,8,4 frames），在4个基准数据集上与3种基线方法对比（表1）。
    2.  在另外4种不同的VLM架构上测试了GIFT作为即插即用模块的效果（表2），覆盖了8和32两种预算。
*   **消融实验组数**：
    1.  模块消融：验证了“定向多样性”和“预算感知细化”两个核心模块的有效性（图3）。
    2.  超参数敏感性分析：对预算感知细化中的批大小 `B`进行了分析（表3）。
*   **公平性与客观性评价**：
    *   使用了统一的评估工具lmms-eval和统一的特征提取器SigLIP以确保公平比较。
    *   对比了当前具有代表性的开源SOTA方法(BOLT, AKS)。
*   **结论**：实验组数充足，涵盖了不同模型、不同数据集、不同预算以及内部模块分析，能够较为全面地验证方法的有效性和鲁棒性。

## 6. 主要结论与发现
1. GIFT在多个长视频理解基准测试中均达到了最先进的性能表现。例如在32个frame budget下平均得分62.9%，优于均匀采样的60.3%。
2.GIFT在极端严格的预算约束下表现尤为突出。例如仅用4个frame budget时仍能保留LLaVA-Video(64frames)原始性能的93.
9%，显著优于其他基线方法.
3.GIFT具有良好的跨模型泛化能力作为即插即用的无训练模块它能稳定提升多种不同架构VLM的性能.
4消融研究表明定向多样性和预算感知细化两个组件均对最终性能有重要贡献缺一不可.

##7优点

###方法论亮点

***全局视角而非贪婪搜索***:通过定义并最大化统一的不可替代性分数避免了传统贪婪决策带来的误差传播问题.

***创新的定向多样性***:将多样性的概念重新定义为条件于相关性的独特性有效防止了为追求视觉差异而引入无关噪声的问题.

***自适应的预算感知细化***:通过迭代更新机制动态调整选择逻辑巧妙地解决了静态评分与时间连贯需求之间的矛盾使算法能随预算增加自然构建更丰富的上下文.

###实验设计亮点

***广泛的评测范围***:覆盖了多种时长、复杂度的视频数据集以及多种主流VLM架构充分证明了方法的通用性和鲁棒性.

***详尽的消融与分析***:不仅验证了整体性能还深入分析了各模块的作用及超参数的影响增强了结论的可信度.

##8不足与局限

###应用限制

***依赖预提取的特征质量***:GIFT的性能依赖于用于计算相关性和视觉相似度的预训练特征提取器(SigLIP)的特征质量若特征无法有效表征特定领域或复杂语义可能影响效果.

***候选池大小固定***:文中统一预采样128个候选这可能限制了其在极长视频(如数小时)中挖掘更细粒度关键信息的能力虽然这是一种常见的效率折衷.

###偏差风险

***时间建模相对间接***:虽然预算感知细化策略有助于构建时间上下文但其本质上是通过释放视觉相似度抑制来实现的对显式的时间顺序或长期依赖关系的建模能力可能弱于专门设计的时序模型.

###实验覆盖

未在更多样化的任务上进行测试如密集视频描述、动作定位等这些任务对时序连贯性的要求可能与问答任务不同.

未详细探讨在不同类型的视频内容(如监控视频、医疗影像)上的泛化能力这些领域可能存在独特的冗余模式和相关信息分布.

(完)

## 9. 潜在改进方向与未来工作
基于论文指出的局限性和方法本身的特点，潜在的改进方向包括：
*   **特征提取器的适配与优化**：探索针对视频内容或特定下游任务微调特征提取器（如SigLIP）的可能性，以提升查询相关性和视觉相似度度量的准确性。
*   **动态候选池构建**：研究自适应的候选帧采样策略，根据视频长度和内容复杂度动态决定初始候选池大小，以平衡计算开销与信息覆盖。
*   **显式时序建模的融合**：考虑将GIFT的不可替代性评分与轻量级的显式时序模型（如时序注意力或图网络）相结合，以更好地处理对长程依赖和严格事件顺序敏感的任务。
*   **扩展到更广泛的任务**：将GIFT框架应用于动作定位、视频摘要、密集描述生成等更多视频理解任务中，验证其在不同范式下的有效性。

## 10. 总结评价
GIFT是一项在视频大语言模型高效推理领域具有重要贡献的工作。它通过引入“全局不可替代性”这一新颖且统一的视角，巧妙地解决了传统关键帧选择方法中“短视决策”与“目标解耦”两大核心痛点。其提出的**定向多样性**概念和**预算感知细化**机制，在理论上优雅，在实践中有效。

实验证明，GIFT作为一个无训练、即插即用的模块，能够在显著降低计算负担（减少输入帧数）的同时，在多种长视频问答基准上保持甚至超越密集采样的性能表现。特别是在极端低预算场景下，其优势更为明显。

尽管该方法在显式时序建模和对特定领域特征的泛化能力上存在一定局限，但其整体框架简洁、有效，为后续研究提供了一个坚实且有启发性的新范式。论文实验充分、分析深入，结论可靠。

（完）
