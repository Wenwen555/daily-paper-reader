---
title: "HORNet: Task-Guided Frame Selection for Video Question Answering with Vision-Language Models"
title_zh: HORNet：面向视觉语言模型视频问答的任务引导式帧选择
authors: "Xiangyu Bai, Bishoy Galoaa, Sarah Ostadabbas"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18850v1"
tags: ["query:lvlm"]
score: 9.0
evidence: 针对视频问答的任务导向型帧选择策略
tldr: "本文提出HORNet，一种基于任务引导的轻量级帧选择方法，用于提升视频问答中视觉语言模型的效率与性能。针对现有系统依赖均匀采样导致效率低下的问题，该方法通过Group Relative Policy Optimization训练帧选择策略，仅需少量参数即可大幅减少输入帧数（高达99%）和处理时间（高达93%），并在多个基准测试中提高了回答准确率（如MSVD-QA上F1提升1.7%），证明了优化视觉输入选择是增强VLM效能的有效途径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.18850v1/fig-001.webp\", \"caption\": \"Figure 1. HORNet pipeline. Given a video V = {v1, v2, . . . , vT } with T uniformly sampled frames, our TimeSFormer-based video encoder E extracts per-frame features F ∈ RT×D . A lightweight trainable MLP policy πθ scores each frame independently, producing keep probabilities pt ∈ [0, 1] and a binary selection mask b ∈ {0, 1}T . Only the frames selected by the mask (V′ ⊆ V) are passed to the frozen Qwen3-VL answerer. For example, to answer “How does the boy in black react while the boy on the green disc goes down?”, HORNet selects only the frames capturing the key interaction moment, discarding irrelevant context; correctly predicting “Look at him”. At training time, GRPO samples K candidate subsets, evaluates each via String F1 reward against the ground-truth answer, and updates πθ through group-normalized policy gradients. The VLM answerer remain frozen throughout while the encoder and MLP policy are trainable.\", \"page\": 2, \"index\": 1, \"width\": 1036, \"height\": 323}, {\"url\": \"assets/figures/arxiv/2603.18850v1/fig-002.webp\", \"caption\": \"Table 3. Performance comparison across selection-based MCQ datasets. We adopt selection-accuracy as our metric and report efficiency gain of HORNet similar to Table 2. For each of these dataset we randomly sampled 1,000 QA pairs.\", \"page\": 7, \"index\": 2, \"width\": 926, \"height\": 183}, {\"url\": \"assets/figures/arxiv/2603.18850v1/fig-003.webp\", \"caption\": \"Table 2. Performance comparison across open-ended QA datasets. We adopt the aforementioned F1-Lev metric to measure model performance, which is a weighted combination of token-level F1 and normalized edit similarity on lemmatized texts. We also report efficiency measured in runtime and average frames passed to Qwen. Qwen’s baseline processing time includes uniform sampling, video encoding, and answer generation. In our setup, frame selection replaces the sampling step and is reported separately. Even under this accounting, the combined runtime of HORNet still yields a notable overall speedup. Additionally, when comparing generation speed, we follow Qwen’s default sampling rate (fps=2). Under the assumption of a 24-fps source video, the baseline effectively processes roughly 1/12 of all framesstill substantially more input than HORNet requires. We highlight best performance for each benchmark in bold and mark performance gain or loss as percentages.\", \"page\": 7, \"index\": 3, \"width\": 882, \"height\": 183}, {\"url\": \"assets/figures/arxiv/2603.18850v1/fig-004.webp\", \"caption\": \"Figure 3. Qualitative example of HORNet’s frame-selection behavior on an MCQ and open-ended sample from the NExT-QA dataset [32]. Given fixed 8-frame input, uniform sampling in Qwen-VL captures frames of the child crawling instead of the slide following the discard of the cart (left), and a frame of a person working at a computer while missing the eating frames (right), leading the model to produce an incorrect answer. With a dense initial sampling (T=256 frames), HORNet selects the full 8-frame sequence of action-relevant frames while discarding distractors, enabling the model to recover the correct prediction.\", \"page\": 8, \"index\": 4, \"width\": 1036, \"height\": 673}, {\"url\": \"assets/figures/arxiv/2603.18850v1/fig-005.webp\", \"caption\": \"Figure 2. HORNet encoder E. Input frames are patchified with a P × P convolution, processed by spatial self-attention within each frame, and then by temporal self-attention across frames at each patch location. The resulting temporally contextualized patch tokens are pooled to yield per-frame video representations used by HORNet for frame selection. B is batch size, T is frame count and D is hidden dimension. We set P=16, T=32 and D=768 in our training.\", \"page\": 4, \"index\": 5, \"width\": 398, \"height\": 406}]"
motivation: 现有视频问答系统依赖均匀或启发式采样，无法针对下游回答质量优化帧选择。
method: 采用Group Relative Policy Optimization训练轻量级帧选择策略，仅需少于100万个可训练参数。
result: "在多个基准测试中，HORNet将输入帧减少高达99%，处理时间降低93%，同时提升回答质量（如MSVD-QA上F1提高1.7%）。"
conclusion: HORNet通过优化帧选择策略，显著提升了视频问答任务的效率和性能，为VLM的高效应用提供了新思路。
---

## 摘要
基于视觉语言模型（VLM）的视频问答（VQA）的性能在很大程度上取决于从输入视频中选择哪些帧，然而大多数系统依赖于均匀或启发式采样，这些方法无法针对下游回答质量进行优化。我们提出了 \textbf{HORNet}，这是一种轻量级的帧选择策略，通过组相对策略优化（GRPO）进行训练，以学习冻结的 VLM 需要哪些帧才能正确回答问题。HORNet 的可训练参数少于 100 万个，能将输入帧减少高达 99\%，并将 VLM 处理时间缩短高达 93\%，同时在短格式基准测试上提高了回答质量（在 MSVD-QA 上 F1 分数提升 +1.7\%），并在时序推理任务上实现了强劲性能（在 NExT-QA 上比均匀采样高出 +7.3 分）。我们将此形式化为“选择任意帧”（SAF）任务，该任务将视觉输入整理与 VLM 推理解耦，并表明 GRPO 训练的选择策略比监督学习和 PPO 替代方案具有更好的分布外泛化能力。HORNet的策略无需重新训练即可在不同 VLM 回答器之间迁移，当与更强的模型配对时能产生额外的8.5\%相对增益。在涵盖341,877个问答对和114.2小时视频的六个基准测试上进行评估后，我们的结果表明，优化 VLM \emph{看到的内容}是一种实用且互补的替代方案，可以在提高效率的同时优化其生成内容。代码可在 https://github.com/ostadabbas/HORNet获取。

## Abstract
Video question answering (VQA) with vision-language models (VLMs) depends critically on which frames are selected from the input video, yet most systems rely on uniform or heuristic sampling that cannot be optimized for downstream answering quality. We introduce \textbf{HORNet}, a lightweight frame selection policy trained with Group Relative Policy Optimization (GRPO) to learn which frames a frozen VLM needs to answer questions correctly. With fewer than 1M trainable parameters, HORNet reduces input frames by up to 99\% and VLM processing time by up to 93\%, while improving answer quality on short-form benchmarks (+1.7\% F1 on MSVD-QA) and achieving strong performance on temporal reasoning tasks (+7.3 points over uniform sampling on NExT-QA). We formalize this as Select Any Frames (SAF), a task that decouples visual input curation from VLM reasoning, and show that GRPO-trained selection generalizes better out-of-distribution than supervised and PPO alternatives. HORNet's policy further transfers across VLM answerers without retraining, yielding an additional 8.5\% relative gain when paired with a stronger model. Evaluated across six benchmarks spanning 341,877 QA pairs and 114.2 hours of video, our results demonstrate that optimizing \emph{what} a VLM sees is a practical and complementary alternative to optimizing what it generates while improving efficiency. Code is available at https://github.com/ostadabbas/HORNet.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对您提供的论文《HORNet: Task-Guided Frame Selection for Video Question Answering with Vision-Language Models》进行结构化、深入且客观的总结。

---

### **论文总结：HORNet**

#### **1. 核心问题与整体含义**
*   **研究背景**：基于视觉语言模型（VLM）的视频问答（VQA）性能严重依赖于从视频中选取哪些帧作为输入。现有方法大多采用均匀采样或启发式采样，这些方法**无法针对下游回答质量进行优化**，导致信息冗余、噪声增加，并可能丢弃关键帧。
*   **核心问题**：如何以任务为导向，智能地选择视频中最具信息量的帧子集，从而在保持甚至提升VLM回答准确率的同时，大幅降低计算开销？
*   **整体含义**：本文提出了一种新的研究范式——将“优化VLM看到什么（输入）”与“优化VLM生成什么（输出）”解耦。通过一个轻量级、可学习的帧选择策略来优化输入，是提升大规模VLM在视频理解任务上效率与性能的一种实用且互补的途径。

#### **2. 方法论**
*   **核心思想**：提出 **Select Any Frames (SAF)** 任务框架。给定一个视频和一个问题，目标是学习一个策略来选择能最大化下游冻结VLM回答准确率的帧子集。该策略通过强化学习直接以问答准确率为奖励信号进行端到端优化。
*   **关键技术细节**：
    1.  **HORNet架构**：
        *   **视频编码器 (E)**：采用轻量级TimeSFormer-Tiny架构提取每帧的时空特征 `F ∈ R^(T×D)`。
        *   **策略网络 (πθ)**：一个极轻量的多层感知机（MLP），输入每帧特征 `ft`，独立输出该帧被保留的概率 `pt ∈ [0,1]`。这是唯一可训练的部分（<1M参数）。
        *   **冻结的VLM回答器 (M)**：如Qwen3-VL-2B，在整个训练和推理过程中保持冻结。
    2.  **训练算法 - Group Relative Policy Optimization (GRPO)**：
        *   对于每个训练样本（视频+问题），策略网络生成K个候选帧选择掩码。
        *   每个候选子集被送入冻结的VLM得到预测答案。
        *   计算奖励 `r(i)`：结合词元级F1分数和编辑相似度来衡量预测答案与标准答案的匹配度。
        *   在K个候选组成的“组”内计算归一化优势 `A(i)` = (`r(i)` - 组内平均奖励) / 组内标准差。
        *   使用GRPO损失函数更新策略网络参数θ，鼓励选择高奖励对应的帧子集。

#### **3. 实验设计**
*   **训练数据**：混合了三个数据集（MSRVTT-QA, MSVD-QA, NExT-QA），总计约15k个视频、22.4万个QA对、114小时内容，覆盖描述性、因果性和时序性等多种问题类型。
*   **评估基准 (Benchmark)**：
    *   **开放式问答**：MSVD-QA, MSRVTT-QA, NExT-OE。
    *   **多项选择题问答**：VideoMME, ActivityNet-QA, NExT-QA MCQ。
*   **对比方法**：
    *   **基线方法**: Uniform Sampling（均匀采样）。
    *   **其他先进方法**: SeViLA, Frame-Voyager, F2C, ReFoCUS, ViaRL等。论文通过表1从“学习型选择”、“奖励优化”、“冻结VLM”、“参数高效”四个维度系统对比了这些方法。

#### **4. 资源与算力**
文中明确提到：“All experiments are conducted on a single NVIDIA A100 40GB GPU.” （所有实验均在单张NVIDIA A100 40GB GPU上进行）。这表明该方法具有较高的计算效率。

#### **5. 实验数量与充分性**
实验设计较为全面和客观：
*   **主实验 (6个数据集)**：在两个任务类型（开放式/多选题）共6个基准上评估了性能与效率增益。
*   **消融实验 (3组)**：
    1.  **训练目标对比**：比较了GRPO与监督学习(SFT)、PPO的性能和分布外泛化能力。
    2.  **选择策略对比**：比较了HORNet与随机采样、均匀采样在固定4帧预算下的性能。
    3.  **VLM回答器迁移性测试**：将训练好的HORNet策略直接应用于更强的Qwen2.5-VL-3B模型，验证其无需重新训练的迁移能力。
*   ***公平性与客观性***：
    *   基线是与使用相同冻结VLM但采用均匀采样的结果进行对比。
    *   报告了全面的指标，包括准确率/F1分数以及关键的效率指标（处理时间、平均输入帧数）。
    *   不仅报告了性能提升的情况（如MSVD-QA），也如实报告了在某些数据集上因大幅减少帧数而导致的性能下降（如VideoMME），并分析了原因。

#### **6. 主要结论与发现**
1.  HORNet能显著提升效率，将输入帧数减少高达99%，VLM处理时间降低64%-93%。
2.  HORNet能在保持甚至提升短视频问答准确率的同时实现上述效率增益（如在MSVD-QA上F1提升+1.7%）。
3.  对于需要复杂时序推理的长视频任务（如NExT-QA），学习型选择策略显著优于随机或均匀采样基线（+7.3分）。
4.  GRPO训练的选帧策略比SFT和PPO具有更好的分布外泛化能力。
5.  学到的选帧策略具有可迁移性，可直接应用于不同的冻结VLM模型而无需重新训练。

#### **7. 优点**
1.  ***创新性***:首次系统地将GRPO用于优化VLMs的“输入”（看什么），而非传统的“输出”（说什么），提出了SAF这一新任务范式。
2. ***高效性***:整个系统仅需训练少于100万个参数的小型MLP和编码器，冻结了大模型主体，非常适合数据稀缺或资源受限的场景。
3. ***模块化与可迁移性***: HORNet作为一个独立的选策模块，可以与任何预训练的冻结VLM配对使用，具有良好的通用性和可移植性。
4.**实验充分**:在大规模、多样化的数据集上进行训练和评估，并通过详实的消融实验验证了各组件设计的有效性。

####8.**不足与局限**
1.**长视频覆盖不足**:对于时长极长的视频(如小时级别的VideoMME)，固定的少量(如8)关键可能不足以覆盖问题的全部时间范围导致准确性下降明显(-16%)。这表明当前方法在处理超长序列时存在局限
2.**固定预算限制**:当前框架为所有视频选择了固定数量的关键这可能不是最优的。未来的工作可以考虑自适应的预算分配根据视频长度或内容复杂度动态调整选择的数目
3.**依赖文本奖励信号**:奖励完全基于最终答案的文本匹配这可能忽略了所选本身的视觉信息质量(例如清晰度信息完整性)。未来可以探索结合视觉感知质量的辅助奖励
4.**未联合微调 V LM**:虽然保持 V LM冻结带来了效率和泛化优势但也限制了 V LM根据精选进行调整以进一步提升性能的可能性

(完)

好的，我将从您上次输出的中断处继续补全总结。

---

#### **8. 不足与局限**
1.  **长视频覆盖不足**：对于时长极长的视频（如小时级别的VideoMME），固定的少量关键帧（如8帧）可能不足以覆盖问题的全部时间范围，导致准确性下降明显（-16%）。这表明当前方法在处理超长序列时存在局限。
2.  **固定预算限制**：当前框架为所有视频选择了固定数量的关键帧，这可能不是最优的。未来的工作可以考虑自适应的预算分配，根据视频长度或内容复杂度动态调整选择的帧数。
3.  **依赖文本奖励信号**：奖励完全基于最终答案的文本匹配，这可能忽略了所选帧本身的视觉信息质量（例如清晰度、信息完整性）。未来可以探索结合视觉感知质量的辅助奖励。
4.  **未联合微调VLM**：虽然保持VLM冻结带来了效率和泛化优势，但也限制了VLM根据精选输入进行调整以进一步提升性能的可能性。

#### **9. 潜在影响与未来方向**
*   **对研究领域的影响**：
    *   **开辟新范式**：将“优化输入”作为提升大模型效率与性能的关键杠杆，为视频理解乃至多模态大模型的高效应用提供了新思路。
    *   **强调任务导向性**：证明了针对特定下游任务（如问答）定制化地选择输入信息比通用的、任务无关的采样策略更有效。
    *   **推动高效方法发展**：展示了通过极小参数量的可学习模块来引导和优化大型冻结模型的可行性，符合当前“以小控大”的研究趋势。
*   **对未来工作的启示**：
    1.  **自适应帧预算**：开发能够根据视频内容和问题复杂度动态决定所需帧数的策略网络。
    2.  **多粒度与层次化选择**：不仅选择关键帧，还可以考虑选择关键片段或关键区域（时空注意力），实现更精细的信息筛选。
    3.  **更鲁棒的奖励设计**：结合视觉质量评估、中间层特征相似度等多维度信号来构建更稳健的奖励函数，减少对单一文本匹配指标的依赖。
    4.  **扩展到其他模态与任务**：该“任务引导的选择”框架可推广至音频、多视角图像等其他时序或冗余数据模态，以及视觉定位、视频摘要等不同下游任务。

#### **10. 总结评价**
本文《HORNet》是一项构思巧妙、执行扎实的工作。它精准地抓住了基于VLM的视频问答中“输入冗余”这一核心效率瓶颈，并提出了一种新颖且高效的解决方案。其核心贡献在于将强化学习中的GRPO算法创造性地应用于优化大模型的“感知前端”，实现了以极低的参数量（<1M）显著提升整个系统效率的目标。实验设计全面、对比公平，不仅展示了方法的有效性，也坦诚地分析了其局限性。

尽管在处理超长视频和自适应预算方面仍有改进空间，但HORNet所倡导的“解耦输入优化与模型微调”、“以任务奖励驱动信息选择”的理念具有很强的启发性和实用性。它为如何在资源受限下高效部署大型多模态模型提供了一个有前景的技术路径。

（完）
