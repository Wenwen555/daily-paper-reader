---
title: "Motion-o: Trajectory-Grounded Video Reasoning"
title_zh: Motion-o：基于轨迹的视频推理
authors: "Bishoy Galoaa, Shayda Moezzi, Xiangyu Bai, Sarah Ostadabbas"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18856v1"
tags: ["query:lvlm"]
score: 9.0
evidence: 以运动为中心的视频理解和基于轨迹的推理
tldr: "本文针对视频理解中物体运动轨迹推理不足的问题，提出了Motion-o框架。该框架通过引入轨迹增强数据集和Motion Chain of Thought推理路径，利用<motion/>标签显式描述物体的方向、速度和尺度变化，从而将离散观察连接成连续轨迹。研究贡献在于形式化了时空轨迹推理能力，提升了模型在时空定位和轨迹预测方面的性能，且无需改变现有架构即可实现。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.18856v1/fig-001.webp\", \"caption\": \"Fig. 3: Motion-o end-to-end pipeline. Starting from the spatio-temporal grounding dataset from [13] with keyframe boxes plus the addition of our dense interpolated bounding boxes (purple box), we (i) compute track-derived motion primitives (direction/speed/scale) and inject them into the reasoning trace as an MCoT <motion/> tag (blue box), (ii) perform supervised fine-tuning (Stage 1) to teach the model the structured STT/MCoT format, and (iii) perform RL (GSPO) (Stage 2) with original vs. motion-masked videos to reward motion tags that align with trajectories and change when temporal evidence is removed.\", \"page\": 6, \"index\": 1, \"width\": 725, \"height\": 406}, {\"url\": \"assets/figures/arxiv/2603.18856v1/fig-002.webp\", \"caption\": \"Fig. 4: Qualitative examples of Motion-o reasoning. Top: The model tracks Sheldon across three timestamps with varying camera angles and emits two <motion/> tags, both correctly identifying the subject as stationary despite significant viewpoint changes. The dense multi-point grounding enables the model to distinguish true stationarity from apparent visual displacement caused by camera cuts. Bottom: The model grounds a duck at four consecutive timestamps as it swims through a group, summarizing the trajectory with a single motion tag capturing eastward direction at moderate speed.\", \"page\": 14, \"index\": 2, \"width\": 725, \"height\": 450}, {\"url\": \"assets/figures/arxiv/2603.18856v1/fig-003.webp\", \"caption\": \"Fig. 2: CoT vs. Motion Chain-ofThought (MCoT). CoT in [13] yields sparse temporal bounding boxes, forcing implicit inter-frame interpolation. MCoT adds an object-conditioned <motion/> tag that explicitly parameterizes the dynamics between observations, making the evidence chain trajectory-consistent and reducing prior-driven extrapolation.\", \"page\": 3, \"index\": 3, \"width\": 320, \"height\": 406}, {\"url\": \"assets/figures/arxiv/2603.18856v1/fig-004.webp\", \"caption\": \"Table 3: Ablation on annotation density and tag representation (Motion-o, with rground). Dense/Discrete represents our default configuration.\", \"page\": 13, \"index\": 4, \"width\": 579, \"height\": 221}, {\"url\": \"assets/figures/arxiv/2603.18856v1/fig-005.webp\", \"caption\": \"Table 2: Performance across video understanding and temporal grounding benchmarks. Bold numbers denote the best results, while underlined numbers indicate the second best.\", \"page\": 13, \"index\": 5, \"width\": 704, \"height\": 335}, {\"url\": \"assets/figures/arxiv/2603.18856v1/fig-006.webp\", \"caption\": \"Table 1: Performance on the V-STAR benchmark, which evaluates spatio-temporal reasoning across three dimensions. Chain1 denotes what–when–where; Chain2 corresponds to what–where–when. mAM is the average of arithmetic mean, and mLGM is the average of modified logarithmic geometric mean, combining temporal and spatial alignment. * indicates re-evaluation using the vLLM framework with 16 sampled frames. Bold numbers denote the best results, while underlined numbers indicate the second best.\", \"page\": 12, \"index\": 6, \"width\": 720, \"height\": 437}]"
motivation: 现有视频推理研究缺乏对物体运动轨迹的显式建模和验证，限制了时空推理能力。
method: "提出Motion-o框架，结合轨迹增强数据集和Motion Chain of Thought推理路径，通过<motion/>标签显式描述物体运动轨迹。"
result: 实验表明Motion-o在时空定位和轨迹预测方面表现优异，且无需修改模型架构即可兼容现有框架。
conclusion: Motion-o通过显式建模物体轨迹，显著提升了视频理解中的时空推理能力，为基于证据的视频理解提供了关键扩展。
---

## 摘要
近期研究在视频推理方面取得了实质性进展，许多模型利用时空证据链来增强其推理能力。与此同时，越来越多的数据集和基准测试提供了结构化标注，旨在支持和评估此类推理。然而，对于物体在观测之间如何移动的推理却鲜有关注：先前的工作均未通过连接连续观测来阐明运动模式，使得轨迹理解隐含且难以验证。我们将这一缺失的能力形式化为时空轨迹（STT）推理，并引入Motion-o——一种以运动为中心的视觉语言模型视频理解扩展，它使轨迹变得明确且可验证。为了支持运动推理，我们还引入了一个轨迹接地数据集工件，该工件通过增强扩展了稀疏的关键帧监督，以产生更密集的边界框跟踪和更强的轨迹级训练信号。最后，我们引入了运动思维链（MCoT），这是一种结构化的推理路径，通过离散的<motion/>标签总结每个物体的方向、速度和尺度（速度）变化，将接地的观测显式地连接成轨迹。为了训练Motion-o，我们设计了一个奖励函数，迫使模型直接基于视觉证据进行推理，同时无需修改架构。实证结果表明，Motion-o改善了时空接地和轨迹预测能力，同时与现有框架完全兼容，确立了运动推理作为基于证据的视频理解的关键延伸。代码可在https://github.com/ostadabbas/Motion-o获取。

## Abstract
Recent research has made substantial progress on video reasoning, with many models leveraging spatio-temporal evidence chains to strengthen their inference capabilities. At the same time, a growing set of datasets and benchmarks now provides structured annotations designed to support and evaluate such reasoning. However, little attention has been paid to reasoning about \emph{how} objects move between observations: no prior work has articulated the motion patterns by connecting successive observations, leaving trajectory understanding implicit and difficult to verify. We formalize this missing capability as Spatial-Temporal-Trajectory (STT) reasoning and introduce \textbf{Motion-o}, a motion-centric video understanding extension to visual language models that makes trajectories explicit and verifiable. To enable motion reasoning, we also introduce a trajectory-grounding dataset artifact that expands sparse keyframe supervision via augmentation to yield denser bounding box tracks and a stronger trajectory-level training signal. Finally, we introduce Motion Chain of Thought (MCoT), a structured reasoning pathway that makes object trajectories through discrete \texttt{<motion/>} tag summarizing per-object direction, speed, and scale (of velocity) change to explicitly connect grounded observations into trajectories. To train Motion-o, we design a reward function that compels the model to reason directly over visual evidence, all while requiring no architectural modifications. Empirical results demonstrate that Motion-o improves spatial-temporal grounding and trajectory prediction while remaining fully compatible with existing frameworks, establishing motion reasoning as a critical extension for evidence-based video understanding. Code is available at https://github.com/ostadabbas/Motion-o.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对这篇题为《Motion-o: Trajectory-Grounded Video Reasoning》的论文进行结构化、深入且客观的总结。

# Motion-o 论文深度分析总结

## 1. 核心问题与整体含义
*   **研究动机**：当前基于证据链的视频推理模型（如 Open-o3 Video）在“何时”（时间）和“何处”（空间）的定位上取得了进展，但它们通常将物体“如何”在观测点之间移动（即**运动轨迹**）视为隐含信息。这导致模型的推理过程不透明、难以验证，且容易依赖静态先验知识进行插值，而非真实的视觉动态证据。
*   **核心问题**：视频理解中缺乏对物体运动轨迹的**显式、结构化建模和验证能力**。
*   **整体含义**：本文形式化了“时空-轨迹”（STT）推理这一新能力，并提出了 Motion-o 框架。其核心贡献在于将视频理解从离散的时空快照（Spatial-Temporal）升级为连续的动态轨迹（Spatial-Temporal-Trajectory），使模型的运动推理变得可解释、可验证。

## 2. 提出的方法论
*   **核心思想**：在现有的时空证据链基础上，引入一个结构化的 **`<motion/>` 标签**作为中间推理步骤，显式地描述连接多个观测点的物体运动属性。
*   **关键技术细节**：
    1.  **Motion Chain of Thought (MCoT)**：
        *   格式：`<motion obj="name" dir="D" speed="S" scale="C"/>`
        *   属性：
            *   `dir` (方向)：8个罗盘方向或静止(STAT)。
            *   `speed` (速度)：静止、慢速、中速、快速。
            *   `scale` (尺度变化)：接近(approaching)、稳定(stable)、远离(receding)，用于表征深度/大小变化。
    2.  **数据增强与监督生成**：
        *   为了解决现有数据集标注稀疏的问题，论文通过插值方法将稀疏的关键帧边界框扩展为密集的轨迹跟踪。
        *   从这些密集轨迹中自动计算每个物体的方向、速度、尺度变化等离散化运动原语，作为 MCoT 标签的监督信号。
    3.  **两阶段训练流程**：
        *   **阶段一（监督微调, SFT）**：使用注入 MCoT 标签的数据对基础视觉语言模型（Qwen2.5-VL-7B）进行微调，使其学会生成结构化的 STT/MCoT 推理链。
        *   **阶段二（强化学习, RL）**：采用 Group Sequence Policy Optimization (GSPO)算法进行优化。奖励函数是关键创新：
            *   `r_traj` (**轨迹奖励**)：评估预测的 `<motion/>` 标签属性与从真实轨迹计算出的地面真值之间的匹配度（考虑相邻类别部分得分）。
            *   `r_ground` (**视觉接地奖励/双链验证**)：比较模型在原始视频和“冻结帧”（移除时序运动线索）视频上生成的 `<motion/>` 标签。如果标签因运动线索消失而改变，则给予奖励；反之则惩罚。这迫使模型依赖真实的视觉动态而非文本先验。

## 3. 实验设计
*   **主要基准测试 (Benchmark)**：
    *   **V-STAR**: 用于评估时空推理能力（What/When/Where），报告 mAM（算术平均均值）和 mLGM（修正对数几何平均均值）。
    *   **VideoMME & WorldSense**: 用于评估通用视频理解能力。
    *   **补充基准测试 (Supplementary)**: TVGBench（时序视频定位）、MVBench（多模态视频理解）、MotionBench（细粒度运动理解）。
*   **对比方法**:
    *   开源模型: Qwen2.5-VL-7B, Video-LLaMA3, LLaVA-Video, InternVL-2.5-8B, TRACE, Sa2VA-8B, Open-o3 Video等。
    *   闭源模型: GPT-4o, Gemini-2-Flash。
*   **主要实验设置**:
    1.  **主实验**: 在两个基线上应用 MCoT——从头训练的 Motion-o (基于 Qwen2.5-VL)和在已训练好的 Open-o3 Video上添加 MCoT (`Open-o3 + MCoT`)。
    2.  **消融实验 (Ablation Studies)**:
        *   移除视觉接地奖励 (`w/o r_ground`)的影响。
        *   比较稀疏标注 vs.密集标注对性能的影响。
        *   比较离散化标签 vs.连续数值标签的效果。

## 4.资源与算力
论文在补充材料中提供了部分实现细节：
*   SFT阶段：在1张 NVIDIA H200 GPU (140GB)上训练了3个epochs。
*   RL阶段：使用了2张 NVIDIA H200 GPU (140GB)。
文中未明确提及总训练时长或具体的数据规模细节。

##5.实验数量与充分性
*   **实验数量充足且设计系统化**: 
    1.在主流的V-STAR、VideoMME等多个基准上进行了全面评估。 
    2.**消融实验设计严谨且具有说服力**: 
      -验证了核心组件(`r_ground`)的必要性。 
      -证明了数据增强(密集标注)的有效性。 
      -通过对比离散vs连续标签的实验，有力地论证了方法设计的选择依据(离散化是有效的关键)。 
    3.**公平性**: 
      -对比了广泛的基线模型(包括开源和闭源)。 
      -在同一基线上进行公平比较(Motion-o vs Qwen基线; Open-o3+MCoT vs Open-o3基线)。 
      -RL阶段使用相同的数据集以确保差异源于方法本身而非数据量。

##6.主要结论与发现
1.Motion-o及其MCoT机制能显著提升模型的时空定位和轨迹预测性能。例如，“Open-o3 + MCoT”在V-STAR上的mAM比原始Open-o3提高了+2.9分；在MotionBench上比GPT-4o高出30分以上。  
2.**显式的轨迹建模不仅不会损害通用视频理解能力(VideoMME)，反而有所提升**,说明这是一种互补的能力扩展。  
3.**密集的运动监督和双链验证奖励对于获得准确且真正基于视觉的运动描述至关重要**,移除它们会导致性能下降(尤其在空间定位上)。  
4.**离散化的运动属性表示比连续数值表示更有效**,更适合语言模型的输出空间和奖励匹配机制。

##7优点
1.**问题定义清晰且有价值**:精准地指出了当前视频推理领域的一个关键空白——缺乏显式的轨迹建模与验证。  
2.**方法轻量且兼容性强**:无需修改模型架构,仅通过改变输出格式和监督信号即可实现能力升级,易于集成到现有框架中。  
3.**创新性的双链验证奖励(`r_ground`)**:这是一个巧妙的机制设计,能有效地区分模型是基于真实视觉动态还是文本先验进行“脑补”,极大地增强了推理的可信度与可解释性。  
4.**详实全面的实验与分析**:覆盖多个基准测试和充分的消融研究,结论支撑有力。

##8不足与局限
1.**空间定位精度依赖基础VLM的能力**:如图S7所示,Motion-o生成的边界框可能不够精确,因为其依赖于底层VLM(Qwen)的空间感知能力而非专用检测器。未来可考虑集成更强的定位模块以提升整体性能的上限。  
2.**运动表示的简化**:目前仅用方向、速度、尺度三个离散属性来描述复杂的物体运动,可能无法捕捉非线性路径或更精细的动态交互(如旋转)。作者也在未来工作中提到了这一点。  
3.**数据集泛化性未充分探讨**:虽然使用了多个基准测试,但未专门讨论其在极端场景(如剧烈遮挡、快速复杂运动)下的鲁棒性表现如何。

---
**(完)**

好的，我将从您提供的“不足与局限”部分之后继续补全总结。

## 9. 潜在影响与未来方向
*   **对视频理解领域的影响**：
    1.  **范式拓展**：Motion-o 将视频推理的范式从“时空快照”推进到“时空轨迹”，为后续研究开辟了新的方向。它强调了**运动作为第一类实体（first-class entity）**进行建模的重要性。
    2.  **可解释性标准**：提出的结构化 `<motion/>` 标签和双链验证机制，为评估模型是否真正“理解”运动提供了可操作的标准和工具，推动了视频AI的可解释性研究。
    3.  **基准构建启示**：这项工作凸显了现有视频问答（VideoQA）数据集在密集、细粒度运动标注上的不足，可能激励社区构建更多包含轨迹级真值的数据集。
*   **未来研究方向**：
    1.  **更精细的运动建模**：如论文所述，未来工作可以探索更复杂的运动原语（如加速度、旋转、非线性路径），甚至引入简单的物理动力学先验。
    2.  **多物体交互推理**：当前框架主要关注单个物体的轨迹。一个自然的延伸是建模多个物体之间的相对运动（追逐、避让、协同），并推理其交互意图。
    3.  **与更强定位模块的集成**：将 MCoT 的思想与高性能的视频目标检测与跟踪（Video Object Detection & Tracking, VOD&T）模型结合，有望在保持强推理能力的同时，大幅提升时空定位的精度。
    4.  **长视频与复杂事件理解**：在更长的时间尺度上，显式的轨迹链可以作为构建复杂事件叙事的关键骨架，帮助模型进行长程因果推理。

## 10. 总体评价
《Motion-o: Trajectory-Grounded Video Reasoning》是一篇高质量、具有前瞻性的研究工作。它没有追求在现有基准上通过堆砌数据或扩大模型来获得边际提升，而是敏锐地识别并形式化了一个被忽视的核心问题——视频中物体运动的显式、可验证推理。

其方法论设计精巧且实用：
*   **问题定义（STT推理）**清晰有力。
*   **解决方案（MCoT标签）**轻量、结构化，易于集成。
*   **训练机制（双链验证奖励）**创新且有效地解决了“脑补”先验知识这一关键挑战。
*   **实验验证**全面、系统，通过主实验和一系列消融研究扎实地证明了每个组件的有效性。

尽管存在对基础VLM定位能力的依赖以及运动表示相对简化等局限，但这些更多指明了未来的改进方向，而非根本性缺陷。本文的核心贡献在于其概念上的突破和方法论上的启发性。它为构建真正理解动态世界、且推理过程透明可信的视频AI系统迈出了坚实的一步。

---
（完）
