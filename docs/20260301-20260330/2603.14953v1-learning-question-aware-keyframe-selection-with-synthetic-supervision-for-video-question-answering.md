---
title: Learning Question-Aware Keyframe Selection with Synthetic Supervision for Video Question Answering
title_zh: 利用合成监督学习问题感知的关键帧选择用于视频问答
authors: "Minchan Kwon, Hyounguk Shon, Junmo Kim"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14953v1"
tags: ["query:lvlm"]
score: 9.0
evidence: 基于LMM伪标签的问题感知关键帧选择
tldr: 大型多模态模型在视频问答中表现出色，但面临推理成本高和信息稀释的挑战。关键帧选择能提高效率，但依赖图像-文本相似性会导致监督稀疏和冗余。本研究提出了一种问题感知的关键帧选择框架，利用从大型多模态模型衍生的伪关键帧标签提供信息监督，并通过覆盖正则化促进跨时间的多样互补证据。在NExT-QA上的实验证明该方法显著提升了准确性，确立了关键帧选择作为视频问答中一个有效且可学习的模块。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.14953v1/fig-001.webp\", \"caption\": \"Fig. 1: An overview of our proposed question-aware keyframe selection framework. The model processes the video with an image encoder and the question with a text encoder. The question embedding is then used in two parallel streams: (1) it is fed into a Gaussian Generator to create an initial keyframe distribution, and (2) it is used to generate synthetic keyframes via CLIP similarity and VLM ranking, which serve as weak supervision. To adapt the selection process, the LMM’s own backbone is re-purposed with a prompt to determine the question type. This information guides the Question-Conditioned Coverage Regularization (QCCR) module, which refines the initial distribution. Finally, the predicted keyframes are passed to the downstream LMM to generate the final answer.\", \"page\": 2, \"index\": 1, \"width\": 1011, \"height\": 369}, {\"url\": \"assets/figures/arxiv/2603.14953v1/fig-002.webp\", \"caption\": \"Table 1: Performance comparison with state-of-the-art methods on the NEXT-QA benchmark. All methods are evaluated on accuracy across three question categories. Our method achieves the highest average accuracy, demonstrating its overall effectiveness.\", \"page\": 3, \"index\": 2, \"width\": 484, \"height\": 411}]"
motivation: 解决视频问答中推理成本高、信息稀释以及现有关键帧选择方法监督稀疏和冗余的问题。
method: 提出了一种包含伪关键帧标签和覆盖正则化的问题感知关键帧选择框架。
result: 在NExT-QA数据集上的实验表明，该方法显著提高了准确性，尤其是在时序和因果问题类型上。
conclusion: 该研究证明了关键帧选择作为视频问答中一个有效且可学习的模块的潜力。
---

## 摘要
大型多模态模型（LMMs）近期在视频问答（VideoQA）任务中展现出卓越性能，但由于推理成本高昂和信息稀释，对视频进行推理仍然具有挑战性。关键帧选择提供了效率和更清晰的推理能力，但在仅依赖图像-文本相似度时，面临着监督稀疏和帧选择冗余的问题。我们提出了一个具有问题感知能力的关键帧选择框架，包含两个组件：源自LMMs的伪关键帧标签，提供信息丰富的监督；以及一个覆盖正则化项，旨在促进跨时间的多样化、互补证据。在NExT-QA数据集上的实验表明，我们的方法显著提高了准确性，特别是对于时序性和因果性问题类型，确立了关键帧选择作为VideoQA中一个有效且可学习的模块的地位。

## Abstract
Large multimodal models (LMMs) have recently demonstrated remarkable performance in video question answering (VideoQA), yet reasoning over video remains challenging due to high inference cost and diluted information. Keyframe selection offers efficiency and sharper reasoning but suffers from sparse supervision and redundant frame choices when relying only on image-text similarity. We present a question-aware keyframe selection framework with two components: pseudo keyframe labels derived from LMMs that provide informative supervision and a coverage regularization that promotes diverse, complementary evidence across time. Experiments on NExT-QA show that our method significantly improves accuracy, especially for temporal and causal question types, establishing keyframe selection as an effective and learnable module for VideoQA.

---

## 论文详细总结（自动生成）

# 论文分析总结：Learning Question-Aware Keyframe Selection with Synthetic Supervision for Video Question Answering

## 1. 核心问题与整体含义
*   **研究背景**：大型多模态模型（LMMs）在视频问答（VideoQA）任务中表现出色，但处理长视频序列时面临两大挑战：
    1.  **高推理成本**：处理大量视频帧导致计算开销巨大。
    2.  **信息稀释**：关键证据被淹没在大量无关或冗余的帧中，影响推理精度。
*   **现有方法局限**：关键帧选择是解决上述问题的有效途径，但现有方法（如基于CLIP的图像-文本相似度排序）存在根本缺陷：
    1.  **监督稀疏**：VideoQA数据集通常只提供问答对，缺乏关键帧的真实标注。
    2.  **忽略时序关系**：基于单帧相似度的选择无法捕捉回答因果、时序类问题所需的跨帧依赖关系。
    3.  **选择冗余**：倾向于选择视觉相似、时间相邻的重复帧，导致证据覆盖不足。
*   **研究动机**：旨在将关键帧选择从一个启发式过程转变为一个可学习的、与问题语义对齐的模块，从而在降低计算成本的同时提升LMMs在VideoQA上的推理质量。

## 2. 方法论
论文提出了一个包含两个核心组件的“问题感知”关键帧选择框架。

*   **整体流程概述**：
    1.  使用图像编码器（如EVA-CLIP）和文本编码器分别处理视频和问题。
    2.  问题嵌入被输入两个并行分支：(a) **高斯生成器 (Gaussian Generator)**，输出初始的关键帧概率分布；(b) **合成关键帧监督 (SKS)** 模块，生成伪标签用于训练。
    3.  利用LMM骨干网络预测问题类型，指导**问题条件覆盖正则化 (QCCR)** 模块优化初始分布，避免冗余并促进时间多样性。
    4.  最终选出的关键帧送入下游LMM生成答案。

*   **关键技术细节**：
    *   **合成关键帧监督 (SKS)**：
        *   为了解决监督缺失的问题，利用一个强大的LMM（如Qwen2.5-VL）作为“教师”，通过精心设计的提示词（Prompts）让其为给定问题和视频生成一组关键帧索引作为伪标签。提示词规则优先考虑信息性、非冗余性、时序逻辑性。
        *   这些伪标签虽然含有噪声，但蕴含了CLIP相似度所缺失的高层语义和跨帧关系信息。训练时结合CLIP相似度生成的弱监督一起使用。
    *   **问题条件覆盖正则化 (QCCR)**：
        *   核心思想是惩罚选择时间上过于集中的帧，鼓励选择能“覆盖”更广时间范围的多样化证据。
        *   引入一个正则化项 `c(I)` （`I`为所选关键帧索引集），与基于问题的得分 `s(Q, x_t)` 共同优化。正则化强度 `λ` 不是固定的，而是根据预测的问题类型进行自适应调整。例如，对于需要多个不同事件状态的因果或时序问题，会施加更强的覆盖约束。

## 3. 实验设计
*   **主要数据集与场景**：
    *   使用 **NExT-QA** 基准数据集进行主实验评估。该数据集包含约5.4K个视频和52K个问答对，平均视频时长44秒。其特点是将问题分为三类进行评估：描述性（Descriptive）、时序性（Temporal）、因果性（Causal），非常适合验证方法在处理复杂推理任务上的能力。
*   **对比方法 (Benchmark)**：
    *   与一系列代表性VideoQA方法进行了对比，包括专注于关系推理的方法（如HCRN, HGA）、改进视频-语言对齐的方法（如IGV）、以及自适应选帧的方法等。
    *   特别对比了两个强相关基线：
        1.  **GCG**: 同样使用高斯生成器进行弱监督选帧的方法。
        2.  **SeViLA**: 一个强大的、在选帧阶段也使用大型模型的LMM-based方法。

## 4. 资源与算力
论文未明确说明训练所使用的具体硬件配置（如GPU型号、数量）、总训练时长或总的计算开销。仅提及了所使用的模型组件及其来源（如InstructBLIP作为LMM骨干，EVA-CLIP作为编码器）。这表明算力需求并非本文的重点讨论或创新点。

## 5. 实验数量与充分性
实验设计较为全面和客观：
*   **主实验 (1组)**：在NExT-QA完整测试集上评估整体性能，并与多个SOTA方法进行公平比较（表1）。
*   **消融实验 (1组)**：系统地移除了两个核心组件——QCCR和SKS——以验证各自的有效性和必要性（表2）。结果清晰显示了每个组件带来的性能增益及在不同问题类型上的影响差异。
*   **定性分析 (示例展示)**：通过具体案例可视化对比了CLIP打分与VLM排序的结果差异（图2），直观展示了所提方法在处理时序/因果问题时选取更相关证据的能力。

总体而言，实验数量适中但聚焦核心主张。通过在标准基准上与多种基线对比并进行消融研究，论证过程较为充分和客观。不过，实验仅在一个数据集上进行验证。

## 6.主要结论与发现
1.  所提出的框架在NExT-QA上取得了最高的平均准确率 (**73.3%**) ，超越了包括SeViLA在内的先前方法。
2.  该方法在所有三类问题上表现均衡且鲁棒。相较于基线GCG，在描述性和因果性问题上有显著提升；相较于SeViLA,则在时序问题上表现更优并取得了更好的平均成绩。
3.  消融研究表明两个核心组件均至关重要：移除SKS导致描述性问题性能大幅下降；移除QCCR则对时序和因果性问题影响明显。这证实了合成监督提供了高层语义指导而覆盖正则化确保了时间多样性。
4.  成功地将关键帧选择从一个简单的检索/启发式过程提升为一个可学习的、能够理解问题语义的模块。

##7 .优点
1.**创新性地解决了监督稀疏难题**:利用大模型自身能力生成伪标签(SKS)，为学习高质量的关键帧选择器提供了宝贵的高层语义信号是一种巧妙的“自监督”思路.
2.**针对性地改进了现有方法的缺陷**:提出的QCCR直接针对基于相似度选法导致的冗余问题进行优化并通过条件于问題类型实现了自适应调节具有很强的针对性.
3.**高效且有效的架构设计**:整个选频模块仅包含一个轻量的高斯生成器和一些规则相比SeViLA等在选频阶段也使用大模型的方法具有明显的计算效率优势同时达到了更优或相当的性能.
4.**详实且聚焦的实验验证**:通过在专门测试时空因果推理能力的NExT-QA数据集上进行充分的主实验和消融分析有力地支撑了论文的核心论点.

##8 .不足与局限
1.**泛化能力有待进一步验证**:所有实验仅在NExT-QA一个数据集上进行虽然该数据集具有代表性但未在其他VideoQA基准(如ActivityNet-QA MSVD-QA)上测试方法的普适性和稳定性存疑.
2.**对“教师”模型的依赖**:SKS的性能依赖于用于生成伪标签的“教师”LMM(Qwen-V-L)的质量如果教师模型本身存在偏见或错误可能会将噪声引入训练过程文中未深入探讨这种误差传播的影响及缓解措施.
3.**问題类型划分可能过于简化**:QCCR依赖于对问題类型的预定义分类(描述/时序/因果)并为每类分配固定强度的正则化参数这种离散的分类可能无法完全捕捉问題语义的复杂性且分类本身的准确性会影响正则化的效果.
4.**未讨论实际部署的计算开销平衡**:虽然强调了选频模块本身轻量但整个流程仍涉及运行两次大模型(一次用于生成伪标签训练一次用于最终答案推理)对于实际应用的总成本效益分析不足.

(完)

## 9. 总结与展望
本论文针对长视频问答中关键帧选择的核心挑战，提出了一个创新且有效的解决方案。其核心贡献在于将关键帧选择从一个依赖启发式规则或简单相似度的过程，转变为一个可学习的、问题感知的模块。通过引入**合成关键帧监督（SKS）** 和**问题条件覆盖正则化（QCCR）** 两个关键技术，该方法巧妙地解决了监督信号缺失和时序冗余选择两大难题。

*   **SKS模块**：利用强大LMM作为“教师”生成伪标签，为训练注入了CLIP相似度所缺乏的高层语义和跨帧逻辑关系信息。
*   **QCCR模块**：通过自适应于问题类型的正则化，强制模型选择时间上更分散、证据更全面的关键帧，有效提升了在因果和时序推理任务上的性能。

实验结果表明，该方法在具有挑战性的NExT-QA基准上达到了新的最优性能，尤其在复杂推理问题上优势明显。其架构设计兼顾了性能与效率，选帧模块本身轻量高效。

### 未来工作展望
基于本文的成果与局限，未来研究可以从以下几个方向展开：
1.  **泛化能力验证**：在更多样化的VideoQA数据集（如ActivityNet-QA, MSVD-QA）上测试方法的普适性，并探索其在开放域视频理解中的潜力。
2.  **噪声鲁棒性增强**：研究更鲁棒的伪标签生成与利用策略，例如通过多教师模型集成、置信度校准或迭代自训练来减轻“教师”模型误差带来的负面影响。
3.  **更精细的问题建模**：超越离散的问题类型分类，探索连续或层次化的问题语义表示，使QCCR等模块能更自适应、更精细地指导关键帧选择。
4.  **端到端优化与效率分析**：探索将选帧模块与下游LMM进行更紧密的联合优化或轻量化设计，并对整个系统的实际部署成本（包括伪标签生成开销）进行全面的评估与分析。

总体而言，这项工作为VideoQA中的高效精准推理提供了一个强有力的新思路，通过让模型学会“看什么”和“怎么看”，显著推进了视频理解技术的发展。

（完）
